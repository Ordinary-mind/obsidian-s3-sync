{
  "designSection": 12,
  "scenarios": [
    {
      "id": 1,
      "evidence": [
        { "test": "test/src/connection-controller.test.ts", "title": "probes first and automatically creates and activates an empty repository scope" },
        { "test": "test/src/connection-controller.test.ts", "title": "rejects ambiguous repository scopes without probing or mutating local settings" }
      ]
    },
    {
      "id": 2,
      "evidence": [
        { "test": "test/src/connection-controller.test.ts", "title": "allows a changed config directory to move to a new Prefix but not to reuse the old repository" },
        { "test": "test/src/connection-controller.test.ts", "title": "rejects a discovered repository bound to a different config directory" }
      ]
    },
    {
      "id": 3,
      "evidence": [
        { "test": "test/adapter-contracts/v1-entrypoint.test.ts", "title": "ships only the current repository runtime" },
        { "test": "test/adapter-contracts/v1-entrypoint.test.ts", "title": "exposes three low-friction commands and a one-click sync ribbon" }
      ]
    },
    {
      "id": 4,
      "evidence": [
        { "test": "test/adapter-contracts/v1-entrypoint.test.ts", "title": "pulls before publishing every pending path and stops on conflicts or pending decisions" }
      ]
    },
    {
      "id": 5,
      "evidence": [
        { "test": "test/core/durable-outbox.test.ts", "title": "replays only verified frozen references in object order and safely retries after a crash" },
        { "test": "test/core/repository-state-transaction.test.ts", "title": "recovers or safely stops after process termination at every Task 5 state boundary" }
      ]
    },
    {
      "id": 6,
      "evidence": [
        { "test": "test/core/durable-outbox.test.ts", "title": "finishes a replay from verified remote objects when local staged content is gone" },
        { "test": "test/core/repository-state-transaction.test.ts", "title": "atomically confirms a terminal Outbox only after an exact remote proof" }
      ]
    },
    {
      "id": 7,
      "evidence": [
        { "test": "test/adapter-contracts/v1-entrypoint.test.ts", "title": "revalidates stale repository identity locks before reading remote conflict candidates" },
        { "test": "test/core/sync-preflight.test.ts", "title": "allows repository identity to be revalidated without creating a sticky recovery lock" }
      ]
    },
    {
      "id": 8,
      "evidence": [
        { "test": "test/core/safe-apply.test.ts", "title": "blocks editor, remote-head, and external-write races at every persisted apply phase" },
        { "test": "test/core/safe-apply.test.ts", "title": "resumes safely after every injected I/O or state boundary" }
      ]
    },
    {
      "id": 9,
      "evidence": [
        { "test": "test/core/remote-vault-conflict.test.ts", "title": "returns stable put and delete candidates for every concurrent head" },
        { "test": "test/core/conflict-resolution-concurrency.test.ts", "title": "keeps an unseen concurrent root as a new conflict after a resolution publishes" }
      ]
    },
    {
      "id": 10,
      "evidence": [
        { "test": "test/core/config-batch-apply.test.ts", "title": "requires exact risk confirmation and rolls back when reloaded Tree differs" },
        { "test": "test/core/config-batch-apply.test.ts", "title": "continues the same batch safely after a crash between an item after-image and Journal progress" }
      ]
    },
    {
      "id": 11,
      "evidence": [
        { "test": "test/core/config-profile.test.ts", "title": "uses the three frozen defaults and rejects raw enable/core/workspace files" },
        { "test": "test/src/vault-event-tracker.test.ts", "title": "does not mutate ignored or otherwise unmanaged paths" }
      ]
    },
    {
      "id": 12,
      "evidence": [
        { "test": "test/src/plugin-data-codec.test.ts", "title": "round-trips the one supported schema without defaults or duplicate runtime Prefix" },
        { "test": "test/src/plugin-data-codec.test.ts", "title": "rejects unsupported, partial, unknown, and identity-inconsistent data" }
      ]
    },
    {
      "id": 13,
      "evidence": [
        { "test": "test/core/safe-error.test.ts", "title": "uses fixed user text instead of raw messages, stacks, causes or secrets" },
        { "test": "test/core/safe-error.test.ts", "title": "reports the exact Outbox recovery stage without exposing a local staging path" }
      ]
    },
    {
      "id": 14,
      "evidence": [
        { "test": "test/adapters/s3-object-store.test.ts", "title": "uses conditional immutable PUT and does not expose DeleteObject" },
        { "test": "test/adapters/s3-object-store.test.ts", "title": "retries throttling with bounded exponential delays and exposes only sanitized diagnostics" }
      ]
    },
    {
      "id": 15,
      "evidence": [
        { "test": "test/simulation/multi-client.test.ts", "title": "randomizes create, edit, delete, rename, offline, reconnect, resolution, restart, and Config snapshots" },
        { "test": "test/simulation/multi-client.test.ts", "title": "converges under reverse/duplicate/partial delivery and exposes missing-parent pending" }
      ]
    },
    {
      "id": 16,
      "evidence": [
        { "test": "test/adapter-contracts/manifest.contract.test.ts", "title": "ships the three root release artifacts without a dist directory" },
        { "test": "test/adapter-contracts/manifest.contract.test.ts", "title": "declares the verified desktop API baseline and excludes unverified mobile runtimes" }
      ]
    }
  ]
}
