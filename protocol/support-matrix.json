{
  "protocol": 1,
  "status": "task-0-test-baseline",
  "runtimes": [
    {
      "name": "Node.js",
      "version": "22.x",
      "status": "supported-for-protocol-tests",
      "requirements": ["TextEncoder", "TextDecoder fatal UTF-8", "BigInt", "SHA-256"]
    },
    {
      "name": "Obsidian desktop",
      "status": "partially-verified-real-contract-tests",
      "staticApi": ["vault.configDir", "vault rename event", "workspace editor-change event"],
      "verified": ["vault.configDir", "workspace editor-change event", "isolated write/readback", "durable write across plugin reload", "rename", "rename rejects an existing target", "copy rejects an existing target"],
      "requirements": ["rename crash-atomicity proof"]
    },
    {
      "name": "Obsidian mobile",
      "status": "not-supported-in-v1-desktop-only",
      "staticApi": ["vault.configDir", "vault rename event", "workspace editor-change event"],
      "requirements": ["mobile support requires a separate runtime-contract and bundle-size validation"]
    }
  ],
  "objectStores": [
    {
      "name": "AWS S3",
      "status": "pending-real-contract-tests",
      "requirements": ["concurrent If-None-Match star atomic create", "GetObject/HeadObject/ListObjectsV2 round trip", "lost-response body verification", "no DeleteObject normal-sync credential"]
    },
    {
      "name": "MinIO or equivalent S3-compatible storage",
      "status": "partially-verified-real-contract-tests",
      "verified": ["If-None-Match star atomic create", "GetObject/HeadObject/ListObjectsV2 round trip", "no DeleteObject normal-sync credential"],
      "requirements": ["pagination disorder and late-visibility handling"]
    },
    {
      "name": "Baidu Cloud BOS S3-compatible storage",
      "status": "partially-verified-real-contract-tests",
      "verified": ["If-None-Match star atomic create", "GetObject/HeadObject/ListObjectsV2 round trip", "no DeleteObject normal-sync credential"],
      "requirements": ["pagination disorder and late-visibility handling"]
    }
  ],
  "nonNegotiableProtocolAssets": [
    "Unicode 15.1.0 NFC and Default Case Folding tables",
    "RFC 8785 canonical UTF-8 JSON validation",
    "SHA-256 immutable object hashes"
  ],
  "unicodeRuntimeSourceBytes": {
    "caseFolding": 18433,
    "nfc": 60120,
    "total": 78553,
    "note": "Source-table measurement; mobile bundle output remains pending adapter release validation."
  }
}
