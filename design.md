# S3 Sync 当前设计

本文只描述当前唯一实现，不定义外部格式导入、多仓库管理或备用同步引擎。

## 1. 产品表面

用户日常只需要三个动作：同步、检查并拉取、状态与检查。

“同步”固定执行先拉后发：先验证仓库、恢复未完成 Outbox、读取远端并处理冲突，再按顺序发布所有已记录的本地变化。只要存在远端冲突、待依赖版本或人工恢复项，本轮就停止发布并打开对应窗口。

连接设置只有一个提交动作“检测并应用”。Bucket + Prefix 中：

- 没有仓库：探测通过后自动创建并接入；
- 恰好一个仓库：验证 descriptor 后自动接入；
- 多个仓库：阻断，不自动选择；
- 当前 `configDir` 与 descriptor 不一致：阻断，并要求使用新 Prefix。

配置中心没有独立总开关。配置快照始终可以检查，但发布和应用始终需要显式操作及风险确认。

## 2. 远端身份与对象布局

仓库身份由以下值共同绑定：

- Endpoint、Region、Bucket、Path-style 和规范 Prefix；
- `repositoryId`；
- `format.json` 的规范 Hash；
- 当前 `configDir` 和历史配置目录排除集合。

远端对象位于 `<prefix>/.obsidian-s3-sync/v1/repositories/<repositoryId>/`。Blob、ConfigTree、Change Chunk 和 Commit 都以规范内容 Hash 命名并且只能条件创建。Commit 最后发布，是一次变化对其他设备可见的边界。

正常运行不调用 `DeleteObject`。probe、历史对象和仓库对象不会被同步流程自动清理。

## 3. 路径寄存器与因果状态

每个 Vault 路径是独立的多版本寄存器；便携配置使用一个完整 ConfigTree 寄存器。

- `observedHeads`：已验证的远端头；
- `projectedHeads`：本机当前字节代表的远端头；
- `basisHeads`：第一次观察到本地变化时固定的父版本；
- `localPredecessorVersion`：同一路径上一份已经冻结的本地版本。

远端头在本地编辑之后到达时，不能替换已固定的 `basisHeads`。Outbox 冻结后发生的新编辑进入下一 generation，并只继承同路径的本地前驱。这样并发不会被错误伪装成顺序修改。

## 4. 本地持久状态

连接和偏好保存在严格 schema 4 的插件 `data.json`。它只包含访问路由、凭证、仓库选择和用户偏好，不保存 Outbox、投影或恢复状态。

仓库运行状态保存在 `<configDir>/.obsidian-s3-sync-local/<repositoryId>/`：

- `state-a.json` / `state-b.json`：带 generation 和校验和的双副本状态；
- `staged/`：发布前不可变内容；
- `outbox/`：待发布对象引用；
- `journals/`：本地应用与配置批次进度；
- `recovery/`：破坏性写入前像；
- `conflict-drafts/`：用户处理冲突时的草稿。

状态写入必须回读验证。双副本内容不合法时先归档状态副本，再建立新的本地 writer；暂存、恢复文件和 S3 对象不删除。普通 I/O 或权限失败不会伪装成损坏，也不会自动归档。

## 5. 发布与 durable Outbox

一次发布按以下顺序执行：

1. 验证仓库 descriptor 与已知 writer frontier；
2. 重放更早的 Outbox；
3. 稳定捕获当前文件到磁盘暂存并验证 size/Hash；
4. 再次读取目标路径的远端头；
5. 在一个本地状态事务中分配 sequence、冻结对象引用和规范 Commit；
6. 按 Blob/Tree、Chunk、Commit 顺序条件写入；
7. 回读并验证所有远端对象；
8. 将 Commit 纳入已验证 frontier；
9. 事务性确认 Outbox，并根据当前本地后像结算或创建下一 generation。

崩溃后只能重放原有 sequence 和已验证的冻结字节。若本地暂存丢失，只有在远端每个对象的 Hash 与大小都精确匹配且 Commit 已进入接受前沿时，才能用只读远端证明完成该 Outbox。不能证明时保持原状态并输出具体 `outboxStage`。

## 6. 拉取与本地安全应用

拉取先验证 descriptor、提交链、Chunk、Tree 和 Blob 依赖，再更新 observed 状态。写本地前需要固定：目标远端头、预期本地前像、投影 generation 和 dirty generation。

安全应用流程为：

1. 持久化 prepared Journal；
2. 把预期前像移动或复制到恢复区并验证；
3. 使用 no-clobber 方式安装目标；
4. 验证安装后像；
5. 再检查编辑事件、远端头和本地 guard；
6. 最后才记账 projection。

任何阶段出现外部编辑、路径占用、磁盘满、权限错误或不完整目录变换，都保留可恢复字节并停止，不直接覆盖。

## 7. 冲突

以下情况形成显式冲突：

- 一个寄存器包含多个并发头；
- 本地变化固定父版本后，远端出现不同头；
- 远端目标与本地未发布内容不同；
- 本地应用期间观察到新的编辑或不受控外部写入。

检查并拉取会先将每个远端 put 候选写入隔离副本，然后自动打开冲突窗口。用户可以打开候选副本、选择本地内容、某个远端版本或删除候选。解决前会重新读取远端头；候选变化则原操作失效。解决提交以当时全部并发头为父版本。

路径大小写别名及文件/目录形状碰撞不得静默选择胜者。安全应用必须停止在可恢复状态。

## 8. 配置通道

配置范围以实际 `vault.configDir` 为根。默认允许 `app.json`、`appearance.json` 和 `hotkeys.json`；workspace、核心插件状态、社区插件启用原文件、本插件目录、本地状态目录和历史 configDir 永久排除。

配置扫描执行两次完整读取，只有 scope revision、路径、size 和 Hash 都一致才产生快照。ConfigTree 冲突按整棵树呈现，不按单文件自动拼接。

配置应用先生成逐文件差异和完整批次计划。插件代码、插件 data、新插件和已加载插件分别要求确认。每个 before-image、staged put 和批次 Journal 都在正式写入前验证；失败时按逆序回滚，遇到并发编辑则进入人工恢复。

## 9. 调度与操作互斥

Vault 与配置操作共享一个可取消的仓库操作锁，不能并发修改状态。自动同步仅决定是否由启动、事件防抖和轮询触发同一个“先拉后发”同步轮次；关闭时仍记录变化，但不自动联网。

网络和限流错误可退避重试。身份、完整性、冲突、本地路径或恢复错误不会当作普通网络问题无限重试。

## 10. 诊断与安全边界

所有用户可见错误都生成可复制的 schema 3 脱敏报告，至少包含稳定 `code`、分类、上下文和 cause 链；连接与同步错误还包含流程阶段。已识别的 S3 失败可包含 operation、stage、HTTP status、Request ID 和重试次数。

报告不得包含凭证、Endpoint、Bucket、Prefix、对象 Key、正文、堆栈或明文 Vault 路径。状态页提供完整脱敏诊断包。

协议没有端到端加密和作者签名，假设所有写凭证持有者可信。配置中的 JavaScript 和 `data.json` 属于高风险明文内容，必须显式确认。

## 11. 明确不做的事情

- 不读取或升级旧共享 manifest；
- 不提供仓库复制、多仓库管理或自动维护删除；
- 不从 S3 控制台对象布局重建可编辑 Vault 镜像；
- 不按 `mtime` 或“最后写入者获胜”自动解决冲突；
- 不支持移动端；
- 不把同步宣传成独立备份；
- 不为未知插件数据 schema、未知远端协议字段或部分配置提供兼容兜底。

## 12. 冻结验收场景

1. 空 Bucket + Prefix 在完整探测后自动创建并接入唯一仓库；非空唯一范围自动验证并接入，多仓库范围直接阻断。
2. `configDir` 变化不能复用原 Prefix；新 Prefix 创建的 descriptor 必须排除此前配置目录。
3. 插件只装载当前仓库运行时，命令面固定为同步、检查并拉取、状态与检查。
4. 每轮同步必须先拉取；冲突或待依赖状态出现时停止发布并打开对应窗口。
5. Outbox 只按 FIFO 重放经过验证的冻结对象；进程在任意发布边界退出都不能复用 sequence 或丢失字节。
6. 本地暂存缺失时，只有精确的全对象远端证明才能把终止 Outbox 收敛为已发布。
7. 仓库身份必须在读取远端冲突候选前重新验证；旧的身份错误不能形成永久恢复锁。
8. 本地安全应用在编辑、远端头、外部写入和 I/O 故障的每个持久边界都停止或恢复，不能静默覆盖。
9. 并发寄存器必须返回每个 put/delete 候选；冲突解决仍要保留未见并发根。
10. 配置应用必须先预览和确认，在崩溃、回滚及并发编辑后保持恢复能力。
11. 配置范围默认仅含三个根级文件，并永久排除 workspace、核心启用状态和本插件。
12. 插件 data.json 只接受一个完整 schema；部分、未知字段和身份不一致全部拒绝。
13. 错误报告必须稳定、脱敏、可复制，并指出连接、前置检查、Outbox 或发布的确切阶段。
14. S3 adapter 使用不可变条件 PUT，不暴露 DeleteObject，并对限流、取消和并发做有界处理。
15. 多设备离线编辑、删除、重命名、乱序/重复提交和并发 ConfigTree 保持确定性且不丢冲突。
16. 发布包只由插件目录根部的 `main.js`、`manifest.json` 和 `styles.css` 组成，manifest 保持桌面限定。
