# 项目背景

开发一个 **Obsidian S3 Sync Plugin**。

目标是替代 Remotely Save，仅支持 **S3 Compatible Storage**，追求稳定、简单、可靠。

---

# 现有方案痛点

### 1. Remotely Save 长期缺乏维护

近两年基本没有功能更新，Issue 回复和修复较少。

---

### 2. 同步算法依赖 mtime

Remotely Save 主要依据：

- mtime
- 文件大小

判断文件是否更新。

而 Obsidian 会在启动时重写部分配置文件（如 `community-plugins.json`、`workspace.json`），即使内容(Hash)完全一致，mtime 也会变化。

导致：

- 启动即认为文件发生修改
- 两台设备互相覆盖配置
- `.obsidian` 配置同步体验很差

---

### 3. 全量扫描 Vault

每次同步都需要扫描整个 Vault。

即使：

```
只修改一个 Markdown
```

也需要重新比较全部文件。

效率低。

---

### 4. 删除同步不透明

用户无法明确知道：

- 删除是否同步
- 为什么没有同步
- 为什么产生冲突文件

行为不可预测。

---

# 插件目标

开发一个：

> **Offline First、Event Driven、S3 Native 的 Obsidian 同步插件。**
