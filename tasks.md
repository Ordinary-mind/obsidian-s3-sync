# S3 Sync 实施状态

更新时间：2026-07-17。

本文件只记录当前项目，不保留旧引擎、迁移方案或已经删除的原型任务。

## 已完成

- [x] 删除共享 manifest 同步引擎、旧 S3 remote、双协调器和仓库重新配置流程。
- [x] 连接流程集中到一个控制器；“检测并应用”自动创建或接入唯一仓库，多仓库直接阻断。
- [x] 仓库发现遇到损坏、不可读或非规范 descriptor 时严格停止，不会把异常 Prefix 当作空仓库。
- [x] Endpoint、Region、Bucket、Prefix、Path-style 和凭证字段具有明确校验错误。
- [x] descriptor 绑定 `configDir` 与历史排除目录；目录变化只能使用新 Prefix。
- [x] 插件配置使用严格 schema 4，不读取部分结构或未知字段。
- [x] 仓库运行状态使用校验和双副本、回读验证和仓库身份隔离。
- [x] Vault create/modify/delete/rename 与 editor-change 都进入持久因果事件。
- [x] 发布使用磁盘暂存、writer reservation、durable Outbox、不可变对象顺序和远端回读确认。
- [x] 旧 Outbox 可按原字节重放；本地暂存缺失时只接受精确远端证明，发布确认检查完整仓库提交状态。
- [x] 拉取先验证 Commit 依赖与仓库身份，再更新 observed 状态。
- [x] 本地应用使用 before-image、Journal、no-clobber 安装、后像校验和恢复区。
- [x] 冲突候选包含每个并发 put/delete 头；副本保留安全扩展名并校验 Hash/大小，拉取或发布竞态都会生成候选。
- [x] 解决冲突前重新读取远端头，并以全部当前并发头为父版本发布结果。
- [x] 配置通道使用完整 ConfigTree、双扫描、逐文件差异、风险确认、批次 Journal 和回滚。
- [x] 配置总开关已删除；配置中心始终可检查，默认范围只含三个安全根级文件。
- [x] 日常命令收敛为“同步 / 检查并拉取 / 状态与检查”，并保留一个同步栏图标。
- [x] 同步固定先拉后发；冲突、待依赖或恢复项会阻断本轮发布。
- [x] 自动同步可关闭，关闭时只记录变化，不自动联网。
- [x] 所有用户可见错误均可复制，包含稳定错误码、脱敏分支 cause、S3 请求元数据和具体流程阶段。
- [x] 状态页可复制完整脱敏诊断包，不包含凭证、正文和明文路径。
- [x] 删除只由测试调用的旧 Outbox、旧 Apply、旧同步状态机、旧内存持久化和冲突解析原型。
- [x] 发布产物固定为仓库根目录 `main.js`、`manifest.json`、`styles.css`，不使用 `dist/`。

## 自动化门禁

- [x] 连接、仓库目录身份和严格插件数据测试。
- [x] durable Outbox 与仓库状态事务故障注入测试。
- [x] 本地安全应用和配置批次故障注入测试。
- [x] 多客户端离线、乱序、重复、冲突和重启确定性模拟。
- [x] S3 adapter 不可变写、分页、流式读取、取消、限流和并发测试。
- [x] 错误脱敏、稳定 code、连接阶段、同步阶段和 Outbox 阶段测试。
- [x] 本次重构后的 `npm test` 全量通过（111 个测试文件，499 项测试）。
- [x] 本次重构后的 `npm run test:types` 通过。
- [x] 本次重构后的 `npm run build` 通过并更新根目录 `main.js`。
- [x] 本次重构后的 `npm run test:ci` 全量通过，官方 npm audit 为 0 漏洞。

## 发布前仍需人工完成

- [ ] 按 [docs/manual-acceptance.md](docs/manual-acceptance.md) 使用两个可丢弃桌面 Vault 逐项验收。
- [ ] 使用专用 Prefix 完成百度 BOS 真实合同测试并保存脱敏证据。
- [ ] 使用本机 MinIO 完成不可变条件写和仓库恢复合同测试。
- [ ] 若要声明 AWS S3 支持，先运行 AWS 合同测试并记录结果。
- [ ] 验证断网、重启、禁用/启用、覆盖升级和中断恢复。
- [ ] 验证新增、修改、删除、重命名、双端离线冲突和第三端收敛。
- [ ] 验证配置预览、应用、回滚、插件代码确认和 ConfigTree 冲突。
- [ ] 记录精确 Bundle Hash、Obsidian 版本、操作系统、供应商和验收结果；不得记录密钥或正文。

## 发布判定

只有自动化门禁全部通过，并且真实对象存储与双 Vault 人工清单完成后，才可以将构建标记为桌面候选版本。当前项目没有移动端发布计划。
